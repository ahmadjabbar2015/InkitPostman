meta {
  name: Guest User List
  type: http
  seq: 1
}

get {
  url: {{baseURL}}/v1/client/:client/guest-users/
  body: none
  auth: bearer
}

params:query {
  ~perPage: 1
  ~perPage: 1
}

params:path {
  client: 1
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
