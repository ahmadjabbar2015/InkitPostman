meta {
  name: Resend OTP
  type: http
  seq: 11
}

post {
  url: {{baseURL}}/v1/auth/resend-otp
  body: json
  auth: inherit
}

body:json {
  {
    "userId": 1
  }
}

script:post-response {
  let body = res.getBody();
  
  let token = body.data.token;
  
  bru.setEnvVar('token',token);
  
  
}
