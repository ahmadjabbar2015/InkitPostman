meta {
  name: Login with password
  type: http
  seq: 13
}

post {
  url: {{baseURL}}/v1/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "SPR-123456",
    "password": "Ahmad@123$REW"
  }
}

script:post-response {
  let body = res.getBody();
  
  let token = body.data.token;
  
  bru.setEnvVar('token',token);
  
  
}
