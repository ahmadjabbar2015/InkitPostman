meta {
  name: Create Passcode
  type: http
  seq: 8
}

post {
  url: {{baseURL}}/v1/auth/update-passcode
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "setPasscode": true,
    "newPasscode": "1234",
    "newPasscode_confirmation": "1234"
  }
}
