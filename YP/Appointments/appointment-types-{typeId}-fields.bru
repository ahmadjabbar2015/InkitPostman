meta {
  name: appointment-types/{typeId}/fields
  type: http
  seq: 4
}

get {
  url: {{baseURL}}/v1/appointment-types/:type_id/fields
  body: none
  auth: bearer
}

params:path {
  type_id: 1
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
